# Интеллектуальный Мультибанковский Финансовый Советник (ИМФС)

Это бэкенд-сервис для веб-приложения "Интеллектуальный Мультибанковский Финансовый Советник". Приложение представляет собой персонального финансового ассистента, который использует **искусственный интеллект на базе больших языковых моделей (LLM)** для агрегации и анализа финансовых данных из различных банков, предоставляя пользователям полный контроль над своими финансами и помогая принимать взвешенные решения.

## 🚀 Ключевые преимущества

- **Единый центр управления:** Подключайте счета из нескольких ведущих банков (VBank, ABank, SBank) и управляйте ими из одного интуитивно понятного интерфейса.
- **Продвинутая аналитика на базе ИИ:** Система автоматически категоризирует ваши траты, выявляет паттерны и помогает понять, куда на самом деле уходят деньги. **ИИ-агенты анализируют эти данные, чтобы предоставить глубокие инсайты и прогнозы.**
- **Персональные советы и автоматизация:** На основе анализа ваших данных ИМФС дает конкретные рекомендации: от оптимизации подписок до советов по накоплению и инвестированию. **ИИ-агенты могут также помочь в автоматизации финансовых задач.**
- **Естественно-языковое взаимодействие:** Общайтесь с вашим финансовым советником на естественном языке, задавая вопросы и получая ответы, как если бы вы разговаривали с живым экспертом.
- **Безопасность прежде всего:** Проект использует современные стандарты шифрования (включая сквозное шифрование для токенов доступа), валидацию JWT/JWKS и работает в строгом соответствии со стандартами OpenBanking.
- **Готовность к будущему:** Модульная архитектура легко расширяется для подключения новых банков, финансовых сервисов и интеграции с новыми LLM.

## 💎 Обзор Функциональных Возможностей

ИМФС предоставляет широкий набор инструментов для управления финансами — от базовых операций до продвинутых ИИ-сервисов.

- **🔑 Подключение банков (`ConnectBanksPage`):** Первый и самый важный шаг. Пользователь предоставляет согласие на доступ к своим счетам в различных банках (VBank, ABank, SBank) в соответствии со стандартами OpenBanking.

- **🏠 Главная панель (`Dashboard`):** Центральный хаб, где собрана вся ключевая информация: общий капитал, список счетов с балансами, диаграмма расходов по категориям, прогресс по финансовым целям и быстрый доступ к AI-ассистенту.

- **✨ AI Ассистент (`Assistant`):** Чат-интерфейс для взаимодействия с финансовым советником на естественном языке. Пользователь может задавать вопросы о своих финансах ("Сколько я потратил на такси в этом месяце?") и получать развернутые ответы и рекомендации.

- **💳 Управление картами и счетами (`CardsPage`):** Наглядное представление всех дебетовых и кредитных карт. Здесь же AI-алгоритм анализирует траты и **рекомендует более выгодные карты** с повышенным кэшбэком для определенных категорий.

### Premium-сервисы на базе ИИ:

-   **Умная оплата (`VBankPay` / `SmartPay`):** Сервис автоматически анализирует условия кэшбэка по всем вашим картам. При совершении покупки он **выбирает наиболее выгодную карту** для оплаты, чтобы вы всегда получали максимальный кэшбэк.
-   **Умное списание (`SmartDebiting`):** Если на основной карте не хватает средств для крупной покупки, сервис **создает оптимальный план списания**, в первую очередь используя свободные средства с других дебетовых счетов, и только затем, при необходимости, подключая накопительные счета, минимизируя потери процентного дохода.
-   **Умный обмен (`SmartExchange`):** При необходимости обменять валюту, сервис **сканирует курсы всех подключенных банков**, чтобы найти и предложить вам самый выгодный курс.
-   **Умное управление подписками (`SmartSubscriptions`):** Автоматически **отслеживает все ваши активные подписки**, предупреждает о предстоящих списаниях и дает возможность **заблокировать платеж в один клик**.
-   **Умные платежи по кредитам (`SmartLoanPayments`):** Сервис не только помогает **оптимизировать ежемесячные платежи** по кредитам, но и постоянно **ищет более выгодные предложения по рефинансированию** ваших займов в других банках.
-   **Платформа доверия (`TrustPlatform`):** Ваш личный AI-аудитор. Он непрерывно **анализирует условия ваших банковских продуктов**, сравнивает их с рынком, выявляет скрытые комиссии, невыгодные ставки и неиспользуемые льготы.
-   **Маркетплейс подписок (`SubscriptionMarketplace`):** На основе анализа ваших трат, сервис **рекомендует подписки, которые могут быть вам выгодны** (например, на такси или доставку еды).
-   **Ночной сейф (`NightSafe`):** Сервис **автоматически переводит свободные остатки** с ваших дебетовых карт на высокодоходный накопительный счет на ночь и возвращает их утром, чтобы вы получали процентный доход.

---

## 🌟 Архитектура и Лучшие Решения

Проект построен на **многослойной архитектуре** и принципах **разделения ответственности (SoC)**. Это обеспечивает высокую модульность, масштабируемость и легкость поддержки.

### 1. Модуль `app/auth_manager` (Централизованный Менеджер Аутентификации)
- **Преимущество:** Вся логика, связанная с получением, кэшированием (in-memory + DB) и обновлением токенов доступа к API банков, инкапсулирована в одном месте. Это **устраняет дублирование кода** и **гарантирует консистентность** политик безопасности. Другие модули получают токен через механизм внедрения зависимостей (`Depends`), не зная о деталях его жизненного цикла. Реализована валидация межбанковских JWT через `JWKS`.

### 2. Модуль `app/banks` (Полиморфный доступ к банкам)
- **Преимущество:** Каждый банк представлен своим "клиентом" (`VBankClient`, `ABankClient`), который наследуется от общего `BaseBankClient`. Внутри каждого клиента сервисы разделены по доменным областям (`accounts`, `payments`, `products`, `cards`). Это делает код **чрезвычайно гибким**: для добавления нового банка достаточно создать новый клиент, реализующий тот же интерфейс, без изменения основной бизнес-логики приложения.

### 3. Модуль `app/mcp` (Сервер Мультибанковского Протокола)
- **Преимущество:** Является "сердцем" мультибанковской функциональности. Абстрагирует сложность взаимодействия с различными банковскими API, выступая в роли посредника (`Adapter`) и унифицируя запросы и ответы. Позволяет выполнять операции сразу с несколькими банками, агрегируя данные в едином формате.

### 4. Модуль `app/llm_integration` (Интеллектуальный слой с ИИ-агентами)
- **Преимущество:** Реализует интеллектуального финансового советника с помощью `LangChain` и локальной LLM через `Ollama`.
- **Принцип работы:** Агент, созданный с помощью `create_react_agent`, использует **инструменты** (`tools`), которые являются обертками над внутренним API (`MCPService`). Это позволяет LLM не просто генерировать текст, а **взаимодействовать с данными пользователя**: получать списки счетов, анализировать транзакции и т.д. Такой подход превращает LLM из простого чат-бота в полноценного участника системы.

### 5. Модуль `app/ui_connector` (Паттерн "Backend for Frontend" - BFF)
- **Преимущество:** UI-приложение взаимодействует только с одним специализированным API-шлюзом (`ui_connector`), который агрегирует данные из всех внутренних сервисов. Это **упрощает логику фронтенда**, **повышает безопасность** (ключи и сложная логика остаются на бэкенде) и **оптимизирует производительность**, так как UI получает все необходимые данные за один запрос.

### 6. Модуль `app/public_ai_adapter` (Адаптер для внешних ИИ)
- **Преимущество:** Предоставляет защищенный "мост" для интеграции с любыми внешними ИИ-платформами (Claude, OpenAI, etc.). Адаптер транслирует их стандартные запросы (`Tool Use` / `Function Calling`) в вызовы внутренних сервисов приложения, обеспечивая безопасность через `X-API-Key` и скрывая сложность внутренней архитектуры.

---

## 🏁 Быстрый старт

### 1. Предварительные требования
- Установленный Python 3.11+
- Установленный `uv` (менеджер пакетов Python).
- Установленный `npm` и `Node.js` для UI.
- (Опционально, для ИИ-функций) Локально запущенный сервер [Ollama](https://ollama.com/download) с загруженной моделью:
  ```bash
  ollama pull gemma3:4b
  ```

### 2. Настройка Бэкенда
1.  **Установите зависимости Python:**
    ```bash
    uv pip sync pyproject.toml
    ```
2.  **Создайте и настройте файл переменных окружения:**
    Создайте файл `.env` в корне проекта и заполните его по примеру ниже.
    ```env
    CLIENT_ID=your_client_id
    CLIENT_SECRET=your_client_secret
    ENCRYPTION_KEY=your_32_byte_long_encryption_key
    DATABASE_URL=sqlite:///./app.db
    PUBLIC_ADAPTER_API_KEY=your_secret_api_key_for_external_ai
    ```
    - `ENCRYPTION_KEY` генерируется командой:
      ```bash
      python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
      ```

3.  **Запустите бэкенд-сервер:**
    ```bash
    uv run uvicorn main:app --reload
    ```
    Сервер будет доступен по адресу `http://127.0.0.1:8000`. Документация API — `http://127.0.0.1:8000/docs`.

### 3. Запуск UI
1.  **Перейдите в директорию `ui`:** `cd ui`
2.  **Установите зависимости:** `npm install`
3.  **Запустите приложение:** `npm run dev`

---

## 🧪 Тестирование
Наш подход к тестированию построен на принципах **надежности, изоляции и комплексной проверки** бизнес-логики. Мы используем `pytest` для создания модульных и интеграционных тестов.

- **Полная Изоляция:** Тесты не делают реальных сетевых запросов к API банков, используя моки. Это делает их **сверхбыстрыми** и **воспроизводимыми**.
- **Тестирование Реальных Сценариев:** Мы тестируем полные пользовательские сценарии (например, "создать согласие -> получить счета").
- **"Живая" Документация:** Пропущенные тесты (`skipped`) служат документацией ограничений "песочниц" банковских API.

Для полного прогона тестов выполните команду:
```bash
PYTHONPATH=. pytest -sv tests/
```

---

## 🗺️ Справка по API

### Служебные и Агрегирующие Эндпоинты
- `POST /api/v1/aggregator/all`: **(BFF)** Получение всех данных для UI.
- `POST /api/v1/public_adapter/tool_calls`: **(Public AI Adapter)** Эндпоинт для вызова инструментов внешними ИИ.
- `GET /health`: Проверка работоспособности сервиса.

### Аутентификация и Согласия
- `POST /api/v1/auth/create-consent`: Создание согласия на доступ к данным.
- `GET /api/v1/auth/consents/{id}`: Получение информации о согласии.
- `DELETE /api/v1/auth/consents/{id}`: Отзыв согласия.

### Данные по счетам
- `POST /api/v1/data/accounts/list`: Получение списка счетов.
- `POST /api/v1/data/accounts/{id}/balances`: Получение балансов счета.
- `POST /api/v1/data/accounts/{id}/transactions`: Получение транзакций счета.

### Платежи
- `POST /api/v1/payments/{bank_name}/create`: Создание платежа.
- `GET /api/v1/payments/{bank_name}/{id}/status`: Получение статуса платежа.
- `POST /api/v1/payments/payment-consents`: Создание согласия на платеж.
- `GET /api/v1/payments/payment-consents/{id}`: Получение информации о согласии на платеж.
- `DELETE /api/v1/payments/payment-consents/{id}`: Отзыв согласия на платеж.

### Продукты и Договоры
- `GET /api/v1/products/products`: Получение каталога продуктов.
- `GET /api/v1/products/products/{product_id}`: Получение деталей продукта.
- `GET /api/v1/products/product-agreements`: Получение списка договоров клиента.
- `POST /api/v1/products/product-agreements`: Открытие нового продукта.
- `GET /api/v1/products/product-agreements/{agreement_id}`: Детали договора.
- `DELETE /api/v1/products/product-agreements/{agreement_id}`: Закрытие договора.
- `POST /api/v1/products/product-agreement-consents/request`: Создание согласия на управление договорами.
- `GET /api/v1/products/product-agreement-consents/{consent_id}`: Детали согласия на управление договорами.
- `DELETE /api/v1/products/product-agreement-consents/{consent_id}`: Отзыв согласия на управление договорами.

### Карты
- `GET /api/v1/cards/{bank_name}/cards`: Получение списка карт клиента.
- `GET /api/v1/cards/{bank_name}/cards/{card_id}`: Получение детальной информации по карте.
